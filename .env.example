# Vayo Aarogya Environment Variables
# Copy this file to .env.local and fill in the values

# JWT Secret - Generate a strong random string for production
# You can generate one using: openssl rand -base64 32
JWT_SECRET=vayo-aarogya-super-secret-key-change-in-production

# Node Environment
NODE_ENV=development

# Database URL (when using a real database)
# DATABASE_URL=postgresql://user:password@localhost:5432/vayo_aarogya

# Email Configuration (for password reset, notifications)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-email@example.com
# SMTP_PASSWORD=your-email-password
# EMAIL_FROM=noreply@vayo.health
